package ede;


import java.util.ArrayList;
import java.util.Stack;

public class QuickSort {

    //Atributos

    private Stack<Tarea> listaTareas = new Stack();

    private ArrayList<Integer> numeros = new ArrayList<>();

    private Integer pivot = null;


    //Getters Setters


    //Métodos

    public Tarea crearTarea

    public void hacerQS(ArrayList numeros) {

        Tarea tarea = new Tarea(0, numeros.size()-1);

        listaTareas.push(tarea);

        while (!listaTareas.isEmpty()) {
            Tarea actualTarea = listaTareas.pop();
            if (actualTarea.getClass().)
        }
    }

    public void hacerRecorrido(Tarea tarea) {


    }


}
